import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Arturo</title>
        <meta name="description" content="Arturo's personal website" />
        {/* TODO: Need to add a favicon */}
        {/* <link rel="icon" href="/favicon.ico" /> */}
      </Head>

      <div>NO CONTENT YET</div>
    </div>
  );
};

export default Home;
